; # template: inc/env.ini v:1.2.0 d:2026-08-11 i:evs 
; !!! Keep this file safe !!!

;----DB-selector: -----------------------------

; Database select Type: "mysql" or "sqlite"
; Uncomment one of following two lines to Aktivate:
;ACTIVE_DB="mysql_config"
ACTIVE_DB="sqlite_config"

[mysql_config]
DB_TYPE="mysql"
DB_HOST="localhost"
DB_USER=""	; Fill in:
DB_PASS=""	; Fill in:
DB_NAME=""	; Fill in:

[sqlite_config]
DB_TYPE="sqlite"
DB_PATH="data/tinycash.sqlite" ; Can calibrate

;----Incomming mails: -------------------------

; 1. SALG: Kopier af egne udstedte kundefakturaer (Penge IND)
IMAP_INVOICE_SERVER=""	; Example: "{mail.xxxx.com:993/imap/ssl/novalidate-cert}INBOX"
IMAP_INVOICE_USER=""	; Fill in:
IMAP_INVOICE_PASS=""	; Fill in:

; 2. INDKØB: Småudgifter, kvitteringer og boner (Penge UD)
IMAP_VOUCHER_SERVER=""	; Example: "{mail.xxxx.com:993/imap/ssl/novalidate-cert}INBOX"
IMAP_VOUCHER_USER=""	; Fill in:
IMAP_VOUCHER_PASS=""	; Fill in:

; 3. INDKØB: Rigtige købs-/leverandørfakturaer (Penge UD)
IMAP_VENDOR_SERVER=""	; Example: "{mail.xxxx.com:993/imap/ssl/novalidate-cert}INBOX"
IMAP_VENDOR_USER="" 	; Fill in:
IMAP_VENDOR_PASS="" 	; Fill in:

;-----OpenAI-Bilagsskanning and translate-----

; OpenAI Vision API Configuration (Receipt Scanning)
OPENAI_API_KEY="" 	; Fill in: if AI-scanning and help-translate has to be used 

;----------------------------------------------

;----Outgoing mail (SMTP sender): ------------
[mail_config]
MAIL_HOST=""		; Fill in:
MAIL_USER=""		; Fill in:
MAIL_PASS=""		; Fill in:
MAIL_PORT=587		; Can be edited
MAIL_FROM=""		; Fill in:
MAIL_FROM_NAME=""	; Fill in:
MAIL_REPLY_TO=""	; Fill in:

;----Backup encryption (master password): -----
;  Master-kodeord til den automatiske krypterede off-site backup.
;  Sendes ALDRIG i mailen og gemmes ALDRIG i databasen. Gem det et sikkert
;  sted (password manager/pengeskab) - mister du det, er backups tabt.
[backup_config]
BACKUP_PASSWORD=""	; Fill in:

; You may need to delete all indented comments to make it work.
